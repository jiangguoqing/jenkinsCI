pipeline {
  agent any
    stages {
        stage ('checkout scm') {
            steps {
                checkout(scm)
            }
        }
        stage('Build') container ('docker') {
            steps {
              sh 'echo "Hello World"'
              sh '''
              echo "Multiline shell steps works too"
              '''
              }
        }
     }
}
