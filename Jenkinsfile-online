pipeline {
  agent {
    kubernetes {
    cloud 'kubernetes'
  }
  }
    stages {
        stage ('checkout scm') {
            steps {
                checkout(scm)
            }
        }
        stage ('unit test') {
            steps {
                container ('docker') {
                   sh 'docker info'
                }
            }
        }
     }
}
