pipeline {
  agent any
    stages {
        stage ('checkout scm') {
            steps {
                checkout(scm)
            }
        }
        stage('Build') {
            steps {
              sh 'docker'
              sh '''
              echo "Multiline shell steps works too"
              '''
              }
        }
     }
}
